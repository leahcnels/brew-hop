import { queryInfo } from 'brew-hop/helpers/query-info';
import { module, test } from 'qunit';

module('Unit | Helper | query info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = queryInfo([42]);
  assert.ok(result);
});
